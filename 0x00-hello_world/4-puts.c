#include <stdio.h>

/**
 * main - is void hence it returns 0 if there is no error
 * It prints to console using the puts function
 * Return: 0 if no error or non zero if there is an error
 */
int main(void)
{
    puts("\"Programming is like building a multilingual puzzle");
    return 0;
}
